# 复现验证记录

本次验证完成于 2026-09-17。以下均为实际执行结果；输入未抽样，模型未缩小。

验证环境：Windows 64 位，Python 3.13.9，`requirements.lock` 锁定全部 35 个直接和传递依赖。随机种子 2026。原始数据和预处理数据由 SHA-256 固定。允许浮点末位差异，比较容差为 `rtol=1e-9, atol=1e-8`。

| 实际检查 | 结果与证据 |
|---|---|
| 新建独立虚拟环境，安装 35 个锁定依赖，`pip check` | 通过；另建 Git 克隆目录后再次独立安装通过 |
| 所有锁定版本可从公开 PyPI 取得 | 35/35 版本 API 返回 200，见 [依赖记录](verification_dependencies.json) |
| 原始压缩包与图像内容 | 官方 MD5 一致；SHA-256 一致；逐张校验 11,788 张图像 |
| 预处理数据、标签、模型与固定参考文件 | 哈希核对通过；元数据和 200 类性状从官方标注与固定快照重建一致 |
| 从完整特征重新训练 | 通过；包括 PowerShell 一键入口，运行约 24 秒，见 [缓存重训记录](verification_cached.json) |
| Git 克隆后从原始图像完整重建 | 通过；含解压/逐图校验、11,788 张图像的全部 68 维特征重提、分类与回归重训、五折与消融，约 245 秒，见 [全量复现记录](verification_full.json) |
| 分类与回归预测 | 5,794 张官方测试图逐条匹配；5,994 张训练集内组外预测逐条匹配 |
| 核心与回归/迁移测试 | 7 项 unittest + 2 项核心图像处理检查通过 |
| 界面与单图推理 | 五个页签正常初始化；合成图与克隆目录中的真实鸟图均完成七项直接回归，见 [界面检查](verification_gui.json) 和 [真实图像界面](verification_real_gui.png) |

上述时间仅是本机记录，首次安装环境、联网下载与其他电脑的运行时间另计。原始压缩包使用本机已有、与官方 MD5 对应的文件；官方下载地址已检查返回 HTTP 200 和正确文件大小，本次没有重复从网络下载整个压缩包。

最终程序版本又在独立克隆目录中完成了提交完整性、数据/模型、GUI 和完整特征重训检查，全部通过；[最终克隆验证记录](verification_final_checkout.json) 保留了被验证的 Git 提交号及各项结果。

输入、模型、测试结果都位于 Git 跟踪内容内。`.venv/`、`data/raw/`、下载缓存及 `build/` 验证临时目录排除在 Git 之外。`scripts/check_submission.py` 另检查已提交版本的必要文件、遗漏项和 100 MiB 大文件限制。

GitHub 云端独立 Windows 环境的验证已全部通过：[运行记录 #35177741802](https://github.com/dissolve-listen/bird-ecology-image-analysis/actions/runs/35177741802)，对应程序提交 `81a175de7a135499966c40a0cc7fe6571bfdce09`。该次运行完成了 Python 3.13.9 配置、依赖安装、提交完整性检查、数据与模型核验、全部测试、GUI 检查以及全部预处理特征的重新训练和数值比对；[云端验证记录](verification_github.json) 保存任务和步骤结果。GitHub 上的 `verification-evidence` 附件保存运行日志和验证输出。

云端执行的是完整特征重训；11,788 张原始图像的重新解压、特征重提和训练已在前述本地独立克隆目录中完成验证。工作流会在每次上传后自动再次运行，最新提交以对应 Actions 结果为准。

本仓库不声称在任意 Python、任意依赖版本或任意操作系统上产生逐位相同的结果。图表受系统字体影响，模型文件受序列化实现影响；验收对象是数据内容、样本划分、视觉特征、分类预测与连续性状预测以及相应评估数值。
